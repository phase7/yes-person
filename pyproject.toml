[project]
name = "yes-person"
version = "0.1.0"
description = "Mock API server driven by OpenAPI specs"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
]

[dependency-groups]
dev = [
    "pytest",
    "syrupy",
    "httpx",
    "ruff",
]
