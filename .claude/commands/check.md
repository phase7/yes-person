---
description: Validates running mock server endpoints locally
---

# /check

This command triggers the `check-endpoints` skill to validate all available mock endpoints.

**To run:**
Type `/check` in your console.
